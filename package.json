{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass style/style.scss:style.css --watch"
  },
  "dependencies": {
    "highcharts": "^11.2.0",
    "@highcharts/dashboards": "^1.2.0",
    "sass": "^1.69.5"
  }
}
